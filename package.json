{
  "name": "abcar-dashboard",
  "version": "1.0.0",
  "description": "ABCAR LineUp Dashboard - Android App",
  "scripts": {
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "build": "echo 'Static app - sync with: npx cap sync android'"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
